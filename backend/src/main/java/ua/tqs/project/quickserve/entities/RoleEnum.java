package ua.tqs.project.quickserve.entities;

public enum RoleEnum {
    CLIENT, MANAGER
    // CLIENT: The user is a client (can make orders)
    // MANAGER: The user is a manager (can manage the restaurant)
}
